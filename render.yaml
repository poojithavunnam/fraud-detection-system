services:
  - type: web
    name: fraud-detection-system
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.0 # Standard stable version for Render
      - key: SECRET_KEY
        generateValue: true
